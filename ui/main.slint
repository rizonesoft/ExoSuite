// ==============================================================================
// ExoSuite - Main Window
// ==============================================================================

import {
    VerticalBox,
    HorizontalBox,
    ListView,
    Button,
    StandardListView,
} from "std-widgets.slint";

export component MainWindow inherits Window {
    title: "ExoSuite - Control Panel";
    width: 1000px;
    height: 700px;
    background: @linear-gradient(180deg, #1a1a2e 0%, #16213e 100%);
    VerticalBox {
        padding: 0px;
        spacing: 0px;
        
        // Toolbar
        Rectangle {
            height: 48px;
            background: #0f3460;
            HorizontalBox {
                padding: 8px;
                spacing: 8px;
                Button {
                    text: "Refresh";
                }

                Button {
                    text: "View";
                }

                Rectangle { }  // Spacer
            }
        }
        
        // Main content area
        HorizontalBox {
            padding: 0px;
            spacing: 0px;
            
            // Sidebar
            Rectangle {
                width: 200px;
                background: #1a1a2e80;
                VerticalBox {
                    padding: 16px;
                    spacing: 8px;
                    Text {
                        text: "Categories";
                        font-size: 14px;
                        font-weight: 600;
                        color: #e94560;
                    }

                    Button {
                        text: "All Items";
                    }

                    Button {
                        text: "System";
                    }

                    Button {
                        text: "Network";
                    }

                    Button {
                        text: "Security";
                    }

                    Button {
                        text: "Display";
                    }

                    Button {
                        text: "Hardware";
                    }

                    Rectangle { }  // Spacer
                }
            }
            
            // Content area
            Rectangle {
                background: #16213e80;
                VerticalBox {
                    padding: 16px;
                    Text {
                        text: "Control Panel Items";
                        font-size: 18px;
                        font-weight: 600;
                        color: white;
                    }

                    Text {
                        text: "No items loaded yet. CPL loader not implemented.";
                        color: #888888;
                    }
                }
            }
        }
        
        // Status bar
        Rectangle {
            height: 24px;
            background: #0f3460;
            HorizontalBox {
                padding-left: 8px;
                padding-right: 8px;
                Text {
                    text: "Ready";
                    font-size: 11px;
                    color: #888888;
                    vertical-alignment: center;
                }
            }
        }
    }
}
