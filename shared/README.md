# Shared Libraries

Common code shared between the main ExoSuite application and extensions.

## Libraries

- **Core/** - Common types, interfaces, utilities
- **UI/** - Shared WinUI3 controls and themes (future)
- **Interop/** - Win32 API helpers (future)

## Usage

Extensions and the main app include headers from this directory and link against the shared libraries.
